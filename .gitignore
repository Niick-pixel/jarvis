.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
config.toml
.env
node_modules/
web/dist/
web/tsconfig.tsbuildinfo

# The runtime directories, anchored to the repository root. Unanchored, `models/` also matches
# `server/models/` and `memory/` matches `web/src/memory/` - which silently kept both source
# packages out of every commit until the voice slice tried to add a file to one of them.
/data/
/models/
/memory/
/services/
